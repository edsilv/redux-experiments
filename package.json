{
  "name": "redux-experiments",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "author": "Edward Silverton e.silverton@gmail.com",
  "license": "MIT",
  "scripts": {
    "del": "rm -r ./.build",
    "build": "tsc",
    "browserify": "browserify ./.build/*.js > ./www/bundle.js",
    "copy": "cp ./src/*.html ./www && cp ./.build/* ./www",
    "serve": "http-server ./www -p 8080",
    "start": "npm run del && npm run build && npm run browserify && npm run copy && npm run serve"
  },
  "dependencies": {
    "@types/jquery": "^3.2.13",
    "@types/redux": "^3.6.0",
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "http-server": "^0.10.0",
    "typescript": "^2.5.3"
  }
}
